{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "(dropdb --if-exists -U dbadmin test-family-tree-db && createdb -U dbadmin test-family-tree-db) && (set NODE_ENV=test) && mocha --exit",
    "testmigrate": "(set NODE_ENV=test) && knex migrate:latest",
    "testrollback": "(set NODE_ENV=test) && knex migrate:rollback",
    "testseed": "(set NODE_ENV=test) && knex seed:run",
    "testmns": "(knex migrate:latest) && (knex seed:run)",
    "migrate": "knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "seed": "knex seed:run",
    "mns": "(knex migrate:latest) && (knex seed:run)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "chai": "^4.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-rate-limit": "^5.1.3",
    "express-slow-down": "^1.3.1",
    "helmet": "^3.23.3",
    "jsonwebtoken": "^8.5.1",
    "knex": "^0.21.1",
    "mocha": "^8.1.3",
    "mongoose": "^5.9.27",
    "morgan": "^1.10.0",
    "multer": "^1.4.2",
    "pg": "^8.2.1",
    "pm2": "^4.4.0",
    "supertest": "^4.0.2",
    "winston": "^3.3.3"
  }
}
